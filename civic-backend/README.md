# CivicPulse standalone backend

This optional Express server implements the same `/api/issues` contract as the built-in Next.js API.

```bash
cd backend
npm install
npm start
```

It runs on `http://localhost:5001` by default and stores demo reports in memory. It requires no credentials or database for local use.
